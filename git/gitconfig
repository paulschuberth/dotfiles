[user]
	name = Paul Schuberth
	email = mail@paulschuberth.de
[core]
	autocrlf = input
	pager = delta

[delta]
	features = side-by-side line-numbers decorations
	navigate = true
	syntax-theme = Solarized (light)

[interactive]
	diffFilter = delta --color-only --light

[includeIf "gitdir:~/Projekte/audi/"]
    path = ~/dotfiles/git/secret-config

[pull]
	rebase = false
