[core]
	autocrlf = input
	pager = delta
	precomposeUnicode = true

[delta]
	features = side-by-side line-numbers decorations
	navigate = true

[interactive]
	diffFilter = delta --color-only --light

[includeIf "gitdir:~/code/audi/"]
    path = ~/dotfiles/git/secret-config

[pull]
	rebase = false
[user]
	name = Paul Schuberth
	email = mail@paulschuberth.de
